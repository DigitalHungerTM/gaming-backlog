info:
  name: games
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_BASE}}/games"
  body:
    type: text
    data: fields name; where id = 12345;
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
