info:
  name: games
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_BASE}}/games.pb"
  body:
    type: text
    data: fields name; search "wreckfest";
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
