info:
  name: games
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_BASE}}/games"
  body:
    type: text
    data: fields id,name; search name "[REDACTED]"; limit 3;
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
